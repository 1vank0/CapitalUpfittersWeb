# Asset Inventory

Status: populated 2026-08-06 from the current-site audit (`reports/existing-site-audit.md`). Media-strategy phase still owns the final reuse/regenerate decisions.

## Known asset locations (repo-verified, 2026-08-06)
- `assets/` — contains only `amp-powerstep.mp4` (2.0MB, homepage hero video — do not change without approval; audited but not flagged for replacement).
- `media/next/` — 19 WebP images, ~1.8MB total: `audience-adventurers-v2.webp`, `audience-contractors-v2.webp`, `audience-fleets-v2.webp`, `audience-public-safety-v2.webp`, `home-hero-v1.webp`, `home-hero-v2.webp`, plus one `-v1.webp` per service (bedliner, camper-shells, ceramic-coating, commercial-wraps, exterior, hitches, industrial-coatings, lighting, mobile-detailing, running-boards, stealth-hitches, suspension, tonneau, toolboxes, undercoating, window-tinting). **Confirmed orphaned: zero references to `media/next` in any HTML or CSS in the live site** (`grep -rl "media/next" *.html services/*.html locations/*.html blog/*.html style.css base.css` → no matches). These are usable candidate assets, not currently live anywhere.
- `og.png` (21.7KB), `og.svg`, `favicon.svg` — brand/social assets at repo root. `favicon.svg` is actively used (linked from every page's `<head>`). **`og.svg` is unreferenced/orphaned.**
- `favicon.old.svg` — unreferenced/orphaned, superseded by `favicon.svg`.
- **Broken reference, sitewide:** every page's `og:image` meta tag points to `https://www.capitalupfitters.com/assets/og-default.png`, a file that does not exist anywhere in the repo (`assets/` contains only the video). 37 occurrences across the codebase. Every social-share preview on the live site is currently broken. See audit §5.1 for full evidence/fix.
- Live site has essentially **zero real photography wired in**: sitewide search found exactly one `<img>` tag in the entire codebase (a JS-templated one in `gallery.html`, only populated if a live third-party fetch returns real URLs). The visual design instead runs on CSS gradients (34 `background-image`/gradient declarations) + 1,258 inline `<svg>` icon elements + the one hero video. This is the actual starting point for the redesign's image strategy — not "existing photography to preserve," but unused WebP assets (`media/next/`) plus a gallery page dependent on a live fetch.
- PR #11 branch (`agent/luxury-homepage-gallery`, not merged, not building on it) has a larger generated media set at `media/gallery/homepage/` (audience, craft, hero, service images in AVIF+WebP, multiple breakpoints) — available for reference/reuse per Ivan's 2026-08-06 decision to reuse existing imagery rather than generate new.
