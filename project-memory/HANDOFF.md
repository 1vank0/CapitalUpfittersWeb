# Handoff

If you are picking this up cold: read `CURRENT-STATE.md`, `REQUIREMENTS.md`, `DECISIONS.md` first, then run `git status` / `git log --oneline -10` / `gh pr list` in the repo to confirm nothing has changed since this was written (2026-08-06).

**Where things stand:** Branch `redesign/2026-relaunch` created, memory scaffold in place, audit + research phases about to launch. Nothing pushed to origin yet. Production untouched.

**Do not** build on PR #11 (`agent/luxury-homepage-gallery`) — Ivan explicitly chose a fresh start on 2026-08-06 (see DECISIONS.md). Leave that PR alone.

**Do not** merge anything or touch production without Ivan's explicit go-ahead.
