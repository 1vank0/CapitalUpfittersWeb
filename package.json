{
  "name": "capital-upfitters-website",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "audit": "node scripts/audit-site.mjs",
    "media:apply": "node scripts/apply-media-system.mjs",
    "repair:markup": "node scripts/repair-static-markup.mjs",
    "test": "node --test tests/*.test.mjs",
    "verify": "npm run audit && npm test"
  },
  "engines": {
    "node": ">=20"
  }
}
